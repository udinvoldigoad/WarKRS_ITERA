# ── Keamanan: JANGAN commit sesi login & data pribadi ─────────────
# Profil browser (berisi sesi login SIAKAD)
.warkrs-profile*/

# Config & state (berisi COOKIE sesi login + target pribadi)
warkrs-config.json
warkrs-state.json
warkrs-debug-captcha.txt

# Konfigurasi opencode (berisi API key MCP)
opencode.json

# ── Dependensi & file sistem ───────────────────────────────────────
node_modules/
*.log
.DS_Store
Thumbs.db

# ── File temp / diagnostik ─────────────────────────────────────────
_*.js
_*.json
_tmp/

captcha-debug/
eng.traineddata

